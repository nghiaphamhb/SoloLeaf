<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOGS" value="./backend/log" />

    <!-- cho phep xuat log console -->
    <appender name="Console"
              class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>  <!-- Dinh dang dong log in ra (color) -->
                %black(%d{yyyy-MM-dd}) %highlight(%-5level) %blue(${PID:-}) %yellow(%logger{40}) : %msg %n
            </Pattern>
        </layout>
    </appender>

    <!-- cho phep ghi log vao file -->
    <appender name="RollingFile"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/spring-boot-logger-2025-11-16.log</file>
        <encoder
                class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd} %-5level ${PID:-} %logger{40} : %msg %n</Pattern>
        </encoder>

        <!-- gioi han to close log's file -> tao log's file moi  -->
        <rollingPolicy
                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 100 MegaBytes -->
            <fileNamePattern>${LOGS}/spring-boot-logger-%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>

    <!-- LOG everything at INFO level -->
    <root level="info">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </root>
</configuration>
